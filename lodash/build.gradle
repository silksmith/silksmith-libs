version = "3.3.1+smith.0"
task github(type: io.silksmith.task.GitFiles) {
    url = "https://github.com/lodash/lodash.git"
    checkout = "3.3.1"
}
silksmith.source {
    main {

        statics {
            srcDir "$github.outputs.files.singleFile"
            include "lodash.min.js"

        }
    }
}
pack {
    manifest {
        statics {
            "lodash.min.js" {
                js()
            }
        }
        usage {
            common {
                files = ["lodash.min.js"]
            }
        }
    }
}

pack.dependsOn github
